{% set seeds = []  %}
{%- for node in groups['cassandra']  -%}
{%- if hostvars[node].seed == "true" -%}
{% set ignored = seeds.append( hostvars[node].primary_ip ) %}
{% endif %}
{% endfor %}
{{ seeds | join(',') }}
